n,k=map(int,input().split())

a=[bin(int(i)) for i in input().split()]
print(len(max(a)))
x=[]

if len(bin(k))>len(max(a)):
    x.append('1')

for i in (2,len(max(a)):
    for j in (0,len(a)):
        if
